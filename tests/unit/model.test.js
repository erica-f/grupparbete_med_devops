import { describe, test, expect, beforeEach } from "vitest";

describe("todo-model", () => {
    beforeEach(() => {
    });

    test("toggleTodo with an invalid id does nothing", () => {

    })
});
